# Pytest configuration for agent-control-plane

[tool:pytest]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"

# Python path for imports
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test*"